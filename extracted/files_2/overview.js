/**
 * analytics/overview.js — вкладка "Обзор"
 */

import { fmtRub, deltaBlock } from './utils.js';

/**
 * Генерирует HTML 4 тайлов (выручка, OPEX, CAPEX, парк)
 * СКОПИРОВАТЬ СЮДА: строки 283-299 из analytics.js
 */
function tilesHtml(summary) {
  // TODO: вставить код функции _tilesHtml
}

/**
 * Генерирует HTML всей вкладки "Обзор"
 * СКОПИРОВАТЬ СЮДА: строки 301-439 из analytics.js
 * 
 * @param {Object} dash - объект дашборда с метриками
 * @returns {string} HTML string
 */
export function renderOverview(dash) {
  // TODO: вставить код функции _overviewHtml
  // Переименовать: _overviewHtml → renderOverview
  // Внутри использует: tilesHtml(summary)
}
