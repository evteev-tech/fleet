/**
 * analytics/kassas.js — вкладка "Кассы"
 */

import { fmtRub } from './utils.js';

/**
 * Генерирует HTML вкладки "Кассы"
 * СКОПИРОВАТЬ СЮДА: строки 843-894 из analytics.js
 * 
 * @param {Object} dash - дашборд с данными касс
 * @returns {string} HTML string
 */
export function renderKassas(dash) {
  // TODO: вставить код функции _kassasRowsHtml
  // Переименовать: _kassasRowsHtml → renderKassas
  // Обернуть в <div class="analytics-page" data-page="4">...</div>
}
